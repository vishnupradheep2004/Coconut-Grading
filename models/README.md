# Model Weights Directory

Place your trained model files in this directory:

- `models/coconut_yolo_v3/weights/best.pt` (or `models/best.pt`) - The trained YOLO model weights.
- `models/price_prediction_model.pkl` - The trained Random Forest Regressor for price estimation.
